// Called when the farmer enters a new space
var bx,by;
bx = argument0;
by = argument1;
// We don't do anything with them -- this is a null function for now.

//field_increasetill(argument0,argument1);
//settill_rwater(argument0,argument1,1);

