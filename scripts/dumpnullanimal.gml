var buildstring, a, subchr;

// Defined in animalbase_create
buildstring = "";
buildstring += numtostring(0); // Doesn't exist

for (a=0; a<68; a+=1) {
  buildstring += numtostring(0);
  }

return buildstring;
