if ( (argument0 >= P_SHADOW_START) and (argument0 < BLDG_START) ) { // Depends on the P_SHADOW_* constants being
  return true; //                       at the end of the plant range.
  }
return false;
    
