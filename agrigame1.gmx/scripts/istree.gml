var species;
species = argument0;
if ((species == T_RED_TOP) or 
    (species == T_RED_BOTTOM) or 
    (species == T_GREEN_TOP) or 
    (species == T_GREEN_BOTTOM)) {
  return true;
  }
return false;
    
